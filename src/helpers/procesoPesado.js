export const procesoPesado = (iteraciones) => {
    // recibo iiteraciones x que quiero hacer

    for (let i = 0; i < iteraciones; i++) {

        console.log('Ahí vamos......');
    }

    return `${iteraciones} iteraciones realizadas`;
}
